# v2 검증 보고서

검증일: 2026-09-22

## 자동 검사

- `node --check app.js`: 통과
- `node test.mjs`: 통과
- 데이터: 137항목, 217학습 단위, ID 중복 없음
- 각 학습 단위의 `lines[].text` 재결합 결과와 저장된 `answer`: 217/217 일치
- 위 검사는 기존 웹 데이터 내부의 무손실을 확인한 것입니다. PDF 지면과 217개를 육안으로 모두 대조했다는 뜻은 아닙니다.

## 실제 브라우저 확인

- 홈과 7개 모드 렌더링, 콘솔 오류 없음
- 폭 1440, 1024, 981, 980, 768, 390, 320px 확인
- 문서 전체 가로 넘침 없음. 390/320px 원문 카드는 고정 360px 조판을 비율 축소하여 컨테이너 안에 표시
- 랜덤 빈칸: 원문 자리의 입력 5개 확인, 첫 칸에서 Tab을 누르면 두 번째 칸으로 이동
- 단계별 가리기: 5단계에서 연도·제목을 포함한 7개 인라인 입력 확인
- 답안 분리: 첫 카드에 `A 카드 전용` 입력 → 다음 카드는 빈 값 → 이전 카드 복귀 시 기존 값 유지
- 모드별 진도 분리: 랜덤 빈칸 2/10 → 질문 카드 1/10 → 랜덤 빈칸 복귀 2/10 확인
- 0번 `먼저 외우기`: 원문 전체 표시와 Enter 다음 카드 이동 1/10→2/10 확인
- 정답 미리 보기: 사용자 입력값을 유지한 채 반투명 오버레이 표시, 입력 칸 포커스 시 자동 가림 확인
- 빈칸 Enter: 첫 칸에서 두 번째 칸으로 포커스 이동 확인
- 공통 `Alt+→`: 질문 카드 1/10→2/10 확인
- 순서 회상: 카드에서 ↓ 입력 시 한 칸 이동하고 같은 카드에 포커스 유지 확인
- 질문 카드 첫 문제: `한인 집단촌은(는) 무엇인가?`와 원문 답 범위 확인
- 연도 양방향: 사건 카드 1개 선택·연도 슬롯 배치 시 트레이 10→9, 슬롯 0→1. 반대 방향 전환 시 연도 카드 10개·사건 슬롯 10개 확인
- 순서 회상: 드래그 가능한 카드 10개와 위/아래 키보드 대안 확인
- `/study/questions` 직접 URL 로드와 새로고침 확인
- 저장 캡처: `work/history-memory-web-v2-qa/`

## 질문 커버리지

217개 단위 모두 런타임에서 질문을 생성합니다.

- 콜론 관계 기반 구체 질문: 78개
- 화살표 관계 기반 순서/결과 질문: 18개
- 나머지 121개: 해당 원문 한 줄에서 핵심 표현 하나를 가린 문장형 질문

정답은 별도 수기 문자열이 아니라 해당 원문 줄에서 파생합니다. 전 질문의 역사적 교육 품질을 교사가 수동 감수한 상태는 아닙니다.

## 확인하지 못했거나 제한된 부분

- 실제 운영체제 한글 IME 조합 이벤트는 자동 입력으로 대체하지 않았으며 미검증입니다. 코드에는 composition 중 재렌더링을 일으키는 키 동작을 두지 않았습니다.
- 터치 기기에서 손가락 드래그는 미검증입니다. 대신 모든 정렬 활동에 선택 후 자리 클릭 또는 위/아래 버튼을 제공합니다.
- 실제 Vercel 공개 배포는 수행하지 않았습니다. `vercel.json`과 직접 경로용 rewrite는 포함했습니다.
- 기존 위치 데이터는 하위 문장의 정밀 좌표가 아니라 상위 항목의 페이지·단 위치일 수 있어 화면에도 `(상위 위치)`로 표시합니다.
- 질문 217개의 문구는 전체 육안 감수를 하지 않았습니다.
